{% extends 'base.html.twig' %}

{% block title %}Hello BlogController!{% endblock %}

{% block body %}

    <article>
        <h2>{{ article.title}}</h2>
        <div class="metatdata">Ecrit le: {{ article.createdAt |date('d/m/Y')}} à {{ article.createdAt |date('H:m')}}</div>
        <div class="content">
            <img src="{{ asset(article.image) }}" style ="width:150px" alt="">
            {{ article.content | raw}}
            <a href="{{ path('blog_show', {'id': article.id}) }}" class="btn btn-primary">lire la suite</a>
        </div>
    </article>

{% endblock %}
